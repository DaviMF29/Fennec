import styled from "styled-components";

const LayoutMainArea = styled.div`
    width: 100%;
    height: 100%;
    background-color: var(--color-primary);
    display: flex;
    flex-direction: row;
`;

export default LayoutMainArea;